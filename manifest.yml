---
declared-services:
  Call for Code Dev:
    label: conversation
    plan: free
applications:
- name: tornado-va
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - Call for Code Dev
  env:
    NPM_CONFIG_PRODUCTION: false
